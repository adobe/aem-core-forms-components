$id: verticaltabs.authoring.schema
$schema: "http://json-schema.org/draft-07/schema#"
title: Vertical Tabs JCR Authoring Properties
description: >-
  Properties for the Adaptive Form Vertical Tabs component (v1).
  All JCR-authored properties for verticaltabs are inherited from PanelImpl
  (readOnly, wrapData, breakBeforeText, breakAfterText, overflowText).
  See panelimpl.authoring.schema.yaml for the full property set.
allOf:
  - $ref: ../panelimpl.authoring.schema.yaml
  - type: object
    properties: {}
